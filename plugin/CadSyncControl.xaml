<UserControl x:Class="CadSyncPlugin.CadSyncControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="#1e293b">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CAD SYNC CLOUD" FontWeight="Bold" Foreground="#38bdf8" FontSize="18"/>
            <TextBlock Text="Fase 4 - Interfaz Activa" Foreground="#64748b" FontSize="11"/>
        </StackPanel>

        <!-- Status & Config -->
        <Border Grid.Row="1" Background="#0f172a" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <DockPanel>
                    <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#22c55e" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusText" Text="Sincronizado" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                </DockPanel>
                <TextBlock x:Name="UrlDisplay" Text="http://localhost:3001" Foreground="#64748b" FontSize="10" Margin="18,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Layer Selector (Modelo A) -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="ÁREA DE TRABAJO" Foreground="#94a3b8" FontSize="10" Margin="0,0,0,5"/>
            <ComboBox x:Name="LayerCombo" SelectedIndex="0" Background="#334155" Foreground="White" BorderThickness="0" Padding="5">
                <ComboBoxItem Content="-- SELECCIONAR CAPA --"/>
                <ComboBoxItem Content="ARQUITECTURA"/>
                <ComboBoxItem Content="ESTRUCTURA"/>
                <ComboBoxItem Content="ELECTRICIDAD"/>
                <ComboBoxItem Content="FONTANERIA"/>
            </ComboBox>
            <Button x:Name="BtnReserve" Content="Reservar Capa Ahora" Margin="0,10,0,0" Background="#38bdf8" Foreground="#0f172a" FontWeight="Bold" Height="35" Click="BtnReserve_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- Activity Log (Alerts) -->
        <GroupBox Grid.Row="3" Header="ACTIVIDAD RECIENTE" Foreground="#94a3b8" BorderBrush="#334155" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="LogList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#334155" BorderThickness="0,0,0,1" Padding="5">
                                <StackPanel>
                                    <TextBlock Text="{Binding Message}" Foreground="White" FontSize="11" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Time}" Foreground="#64748b" FontSize="9"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Actions -->
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="ARCHIVOS DISPONIBLES" Foreground="#94a3b8" FontSize="10" Margin="0,0,0,5"/>
            <ComboBox x:Name="FileCombo" Background="#334155" Foreground="White" BorderThickness="0" Padding="5" Margin="0,0,0,10">
                <ComboBoxItem Content="Cargando archivos..."/>
            </ComboBox>
            <Button x:Name="BtnPull" Content="BAJAR SELECCIONADO" Height="30" Background="#475569" Foreground="White" FontWeight="SemiBold" Click="BtnPull_Click" Margin="0,0,0,10">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Button x:Name="BtnPushDelta" Content="SUBIR MI CAPA" Height="35" Background="#10b981" Foreground="White" FontWeight="Bold" Click="BtnPushDelta_Click" Margin="0,0,0,10" IsEnabled="False">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="BtnSync" Content="SUBIR TODO (FULL PUSH)" Height="35" Background="#818cf8" Foreground="White" FontWeight="Bold" Click="BtnSync_Click">
                 <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
